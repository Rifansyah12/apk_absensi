import 'package:flutter/material.dart';

class AbsensiCheckInPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Absen Masuk")),
      body: Center(child: Text("Form Absen Masuk")),
    );
  }
}
